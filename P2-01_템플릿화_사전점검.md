# P2-01 대표 HWPX 템플릿화 사전점검

- **상태**: `BLOCKED`
- **점검일**: 2026-09-15
- **대상 원본**: `교복4차 (2)(1)/교복 매뉴얼 작성서식0908(안정구 추가).hwpx`
- **원본 SHA-256**: `A5546319F7A6917CA26BA922C9803DFE6B2EC0DA6C9EA9D6A7AB96655BF07649`
- **POC 대상**: F-007, F-024, F-013

## 1. 직접 확인 결과

| 점검 | 결과 | 근거 |
|---|---|---|
| 원본 변경 여부 | 변경 없음 | SHA-256이 P0 기준값과 일치함 |
| 제공 HWPX 구조화 파싱 | 통과 | 4개 HWPX 모두 Kordoc JSON 파싱 종료 코드 0 확인 |
| F-007·F-024·F-013 포함 문서 | 단일 복합 매뉴얼 | `교복 매뉴얼 작성서식0908(안정구 추가).hwpx` 및 `-1.hwpx`는 710개 블록에 복수 서식을 함께 포함함 |
| 독립 대표 서식 파일 | 미확보 | 제공된 4개 HWPX 중 F-007·F-024·F-013을 각각 단독 문서로 분리한 HWPX가 없음 |
| 실제 입력 표식 | 미확정 | `fill --dry-run`은 문서의 표·문단 값을 후보 필드로 반환하나, Form ID별 고유 토큰·반복 표 경계를 확정하지 못함 |

## 2. 차단 사유

Kordoc의 `fill`은 동일 라벨을 문서 전체에서 함께 치환할 수 있음. 현재 원본은 여러 서식을 포함한 복합 매뉴얼이므로, Form ID별 독립 사본·고유 토큰을 만들지 않은 채 마스킹 시험값을 채우면 다른 서식의 같은 표식까지 변경할 위험이 있음.

따라서 현재 파일만으로 F-007·F-024·F-013 각각의 치환·저장·재열기 POC를 안전하게 수행할 수 없음. 원본을 전역 치환하거나 실제 계약·개인정보 값을 넣는 방식은 수행하지 않음.

## 3. 재개 조건

다음 중 하나가 충족되면 P2-01을 재개함.

1. Form ID별 독립 HWPX 원본 또는 승인된 사본을 제공받음.
2. 복합 매뉴얼에서 F-007·F-024·F-013 구간을 독립 HWPX로 분리하는 재현 가능한 방법을 검증함.
3. 고유 토큰·반복 표 범위가 확정된 사본 템플릿을 제공받음.

재개 후에는 가상 학교명·가상 품목·마스킹 역할표시만 포함한 JSON으로 `fill -j`를 수행하고, 생성본 `validate`, 재파싱, 렌더, 한컴 수동 열기와 PDF 비교를 순서대로 수행함.
