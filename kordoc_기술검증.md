# Kordoc 기술검증 — P0-02

- **상태**: `DONE — P0 기술 가능 범위 확인 완료; 실제 값 치환·조판 검증은 P2에서 수행`
- **검증일**: 2026-09-15
- **도구**: `npx -y kordoc@^4`
- **대상 원본**: `교복4차 (2)(1)/교복 매뉴얼 작성서식0908(안정구 추가).hwpx`
- **작업 사본**: `artifacts/hwpx/P0-02_원본사본.hwpx`
- **원본 SHA-256**: `A5546319F7A6917CA26BA922C9803DFE6B2EC0DA6C9EA9D6A7AB96655BF07649`

## 1. 실행 결과

| 기능 | 명령 범주 | 결과 | 증빙 |
|---|---|---|---|
| 구조 검증 | `validate` | PASS — 엔트리 57개 | 사본·왕복본 모두 구조 검증 통과 |
| 필드 탐색 | `fill --dry-run` | PASS — 필드 목록 반환 | 원본 템플릿 표식 유무·중복 라벨을 P1에서 재분류 필요 |
| 구조화 파싱 | `--format json` | PASS | `_workspace/02_hwpx/P0-02_구조.json` 생성 |
| Markdown 파싱 | 기본 파싱 | PASS — 이미지 46개 추출 | `_workspace/02_hwpx/P0-02_왕복.md`, `images/manifest.json` 생성 |
| 서식 보존 왕복 | `patch` | PASS — 0개 변경, 714블록 일치 | `artifacts/hwpx/P0-02_왕복검증.hwpx` 생성 |
| SVG 렌더 | `render` | PASS — 87페이지 렌더 | `_workspace/02_hwpx/P0-02_미리보기.svg` 생성 |
| 실제 값 채움 | `fill -j` | NOT_RUN | 대표 3종·안전한 테스트 필드·반복 표 규칙 확정 전 |
| PDF 비교·한컴 열기 | 수동 검증 | NOT_RUN | P2-02에서 행정실·한컴 환경으로 수행 |

## 2. 관찰한 제약

- 렌더 과정에서 페이지에 걸친 표는 분할 지오메트리가 없어 시작 페이지 영역만 기록한다는 경고가 발생함. 표가 많은 서식은 한컴 열기와 PDF 육안 비교가 필수임.
- `fill --dry-run`은 입력 후보를 탐색하는 단계일 뿐, 안전한 값 치환과 반복 표 처리를 검증하지 않음.
- 명령행 한글 출력은 현재 PowerShell 코드페이지에서 일부 깨져 보였으나, 종료 성공·산출물 생성·`validate` 결과로 파싱 성공을 재확인함. JSON 값 해석은 UTF-8 지원 환경에서 수행해야 함.
- 원본과 사본 SHA-256이 일치함. 원본을 수정하지 않았음.

## 3. 다음 게이트

1. P1에서 단순 공문·반복/계산·복합표를 대표 POC 3종으로 선정함.
2. 마스킹된 테스트 JSON으로 `fill -j`를 수행하고 새 파일을 생성함.
3. `validate`, 재파싱 비교, 한컴 열기, PDF 비교를 수행함.
4. 위 검증이 모두 통과한 뒤 P2-03에서 Excel 연계 또는 웹앱 HWPX 출력 이관을 ADR로 확정함.

## 4. P0-02 판정

Kordoc의 파싱·필드 탐색·fill dry-run·patch·validate·render 가능성은 사본에서 확인함. 실제 값 채움과 한컴/PDF 조판 검증은 대표 3종·마스킹 테스트값을 확정한 P2의 생성 품질 게이트로 분리함. 따라서 P0-02의 기술 가능 범위 검증은 완료함.
