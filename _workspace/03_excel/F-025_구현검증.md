# F-025 교복 A/S 계획서 구현·검증

- **상태**: `DONE — P3-01의 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-18
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260918_v2.xlsm`(v1도 동일 코드로 통과했으나 PDF 육안 확인 이후 문서 갱신 Edit들이 트리거한 훅 백그라운드 재검증이 실패해 `verify_v1_log.txt`가 삭제된 채 남음 — F-023 때와 동일한 알려진 훅 레이스 현상. 충돌 프로세스 없음을 재확인한 뒤 안전 빌드를 한 번 더 단독 실행해 v2와 클린 로그(185줄, PASS 156·FAIL 0)를 재확보함. 코드 변경 없이 재빌드한 것이므로 v1 대상 PDF 육안 확인 결과는 v2에도 그대로 유효함)

## 경위

직전 세션(같은 날짜)이 시트·VBA·검증 스크립트 코드 작성까지만 마치고 사용자 요청("작업 멈춰줘")으로 중단함. 이번 세션은 그 코드를 재검토하고 안전 빌드·PASS 확인·PDF 육안 확인을 완료하는 것으로 시작함.

## 원본 대조

- 원본 HWPX `[9-14]`(`artifacts/hwpx/P0-02_원본사본.hwpx`)를 대조한 결과 표가 아닌 순수 텍스트 서식임을 확인함. 구성: 제목 "교복 A/S 계획서" → "1. A/S 기간"(가. 무상 A/S 기간) → "2. A/S 편의성"(가~라. 사업장 소재지·신속성·출장 A/S·택배 A/S) → "3. A/S지정업체"(가~다. 주소·연락처·거리접근성) → "4. A/S 내용"(가~다. 시설현황·무상/유상 A/S 세부내용) → 날짜·서명란("제안자 성 명(서명 또는 날인)")·수신처("◯◯◯◯학교장 귀하").
- F-021~F-023과 달리 "(제안자: 업체명)" 같은 업체명 표시 자리가 원문에 전혀 없음. 업체를 식별하는 문구 자체가 없는 순수 텍스트 서식이라는 점이 이 서식의 핵심 특징임.

## 매핑표 관찰(기록만, 미수정)

- `서식_매핑표.md`의 F-025 행이 F-026 행과 입력·출력 열이 완전히 동일함(비고만 다름) — 템플릿 복사 흔적으로 추정됨. 다만 F-025는 우연히 원문(업체명 표시 없음)과 매핑표의 출력 열(R-03 제외, C-01/C-02/B-01/D-02만)이 결과적으로 부합함. 매핑표 자체는 P1 분석 산출물이라 수정하지 않고 기록만 남김(P1 재검토 과제).

## 구현 범위와 개인정보 경계

- `scripts/build_excel_v1_structure.ps1`에 `F-025_교복AS계획서` 시트를 F-023 뒤에 추가함(`$implemented` 해시에 `"F-025" = "F-025_교복AS계획서"` 등록). 선택 업체 순번은 `G3`(F-021·F-023과 동일 위치)에 배치하되, 시트 본문 어디에도 업체명 텍스트는 출력하지 않음.
- `scripts/build_excel_v1_vba.ps1`에 F-025 선택출력 분기(업체별 반복), `검증_F025출력가능`/`F025업체행완전한가`/`F025임시시트생성` 함수, 임시 시트 정리 목록(`F025_` 접두어)을 추가함. `F025업체행완전한가`는 F-017·F-019~F-023과 동일하게 `기초자료입력` 업체 반복행(B44:B53)의 업체명 존재 여부만 검사함 — 이 서식 자체는 업체명을 출력하지 않지만, F-025도 업체별 제출 서류(F-021 첨부 목록 ④)이므로 등록된 업체 수만큼 별도 인쇄본을 생성하는 선택 출력 구조는 F-017~F-023과 동일하게 유지함(내용은 업체마다 동일한 학교 공통정보이지만 출력 매수는 업체 수를 따름).
- A/S 기간·편의성·지정업체·내용 전 항목(무상 A/S 기간, 사업장 소재지, 신속성, 출장/택배 A/S, A/S지정업체 주소·연락처·거리접근성, 시설현황, 무상/유상 A/S 세부내용)은 업체가 직접 작성하는 완전한 공란으로 유지함. 날짜(B29)는 `기초자료입력!C5`(학년도)를 참조해 "20○○년 월 일" 형식으로, 수신처(B31)는 `기초자료입력!C4`(학교명)를 참조해 자동 반영함. 이 서식 자체에는 개인식별정보 필드가 없음.

## 자동 검증

- `scripts/verify_excel_v1.ps1`에 다음을 추가함: 필수 시트 27→28개(F-025 포함), A4 자연 배율 목록에 `F-025_교복AS계획서` 추가, F-025 원문 대조 3건(제목·4개 항목 머리글 존재, 업체명 표시 자리 없어 학교 공통정보만 반영·선택 출력 허용, 본문 어디에도 업체명 미출력), PDF 선택 목록에 F-025 추가, 업체별 임시 인쇄 시트 정리 검증 1건.
- `run_build_excel_v1_structure_utf8.ps1` 단독 실행(사전에 좀비 EXCEL.EXE PID 26012 확인·종료 후 실행)으로 `verify_v1_log.txt` 전 항목 PASS(FAIL 0건) — F-007·F-014~F-024·DB 저장/수정/불러오기 회귀 없음 포함.
- F-025 A4 1쪽 자연 배율 확인: `Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=0`, `VPageBreaks=0`.

## 좀비 프로세스 확인

- 작업 시작 전 `Get-Process EXCEL`로 잔여 EXCEL.EXE 1건(PID 26012, 당일 07:28 시작)을 발견해 종료함. 안전 빌드·QA 스크립트 실행 후에도 잔여 프로세스가 없음을 각 단계마다 재확인함.

## PDF 육안 검토

- 비식별 사본(`artifacts/excel/_diagnostic/F-025_20260918_qa/F-025_QA.xlsm`, 검증 완료 후 삭제)에서 필수값(`기초자료입력!C4`=검증초등학교, `C5`=2026, `C12`=2026학년도 동복 학교주관구매, `C22`=2026학년도 동복 학교주관구매 요청)과 업체명(`B44`=검증업체가)만 입력하고 `서식선택_출력`에서 F-025만 TRUE로 설정해 `검증_선택서식_PDF저장` 매크로로 출력함.
- `output/선택서식_20260918_193246.pdf`(76,637바이트, 1쪽)를 200dpi PNG로 렌더링해 직접 확인함: 제목 "교복 A/S 계획서", "1. A/S 기간"~"4. A/S 내용" 4개 항목 머리글과 세부 문구(가~라, 가~다 등)가 원문과 일치함. 본문 어디에도 업체명("검증업체가")이 출력되지 않음(원문에 업체명 표시 자리가 없다는 사실과 부합). 날짜는 "2026년   월   일", 수신처는 "검증초등학교장 귀하"로 학교 공통값만 자동 반영됨. 나머지 전 항목(무상 A/S 기간, 사업장 소재지, 신속성, 출장/택배 A/S, 지정업체 주소·연락처·거리접근성, 시설현황, 무상/유상 A/S 세부내용, 서명란)은 전부 공란으로 A4 1쪽 안에서 잘림 없이 표시됨.
- 진단용 Excel COM 프로세스는 스크립트 종료 후 잔여 없음을 확인함. 비식별 QA 사본(`F-025_20260918_qa/` 디렉터리 전체)은 육안 확인 완료 후 삭제함. 사용자의 실제 작업 파일은 이 세션 전체에서 열거나 수정하지 않음.

## 제한 사항

- F-025 완료는 P3-01 전체 완료를 뜻하지 않음. F-013 및 나머지 39개 Form ID가 남아 있으며 X-06은 `FAIL`을 유지함.
- `서식_매핑표.md`의 F-025 행(F-026과 동일 문구) 불일치는 이번 세션에서 기록만 하고 수정하지 않음 — P1 분석 담당자의 확인·수정이 필요함.
- 상단 "[검토용] 원본 HWPX [9-14] [서식 8] 대조..." 안내 문구는 F-017~F-023과 동일한 기존 패턴대로 인쇄 영역에 포함되어 PDF에 함께 출력됨(새로 도입한 것이 아니며 별도 변경 요청 없어 유지함).
