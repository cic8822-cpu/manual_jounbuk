# F-044 사전 안내 가정통신문 안내 구현·검증

- **상태**: `DONE — 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-19
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260919_v12.xlsm`(구조·VBA·검증 스크립트 전체 재실행, `verify_v1_log.txt` 전 항목 PASS)

## 원본 대조

- 원본 HWPX 73쪽 `[17]`를 kordoc `parse_pages`로 대조함. 단일 물리 페이지.
- 구조: 제목 "신입생 교복구매 사전 안내 가정통신문 발송" → 문서번호/시행일자 → 수신(내부결재(사업부서))
  → (경유) → 제목(문서 제목 공통값) → "1. 관련"(고정 근거 문구) → "2. 발송 사유" 서술문 → "가. 대상"
  → "나. 내용" → 붙임 → 결재란(담당/협조자/교장, 시행/접수, 우편번호/주소/전화/팩스/이메일).

## 매핑표 재확인

- `서식_매핑표.md` F-044 행: `C-01~C-06, B-01~B-02, B-07, D-01~D-05` 입력, `C-01, C-02, B-01, D-02` 출력,
  계산 없음, "안내 대상은 비식별 설명만 사용" 비고. 원문과 불일치 없음.

## 반영 범위와 재사용 판단

- **학교명(G2)**: `기초자료입력!C4` 재사용, 미입력 시 "○ ○ 학 교".
- **문서번호(C5)·시행일자(F5)**: `기초자료입력!C9`, `C8`(TEXT `yyyy-mm-dd`) 재사용.
- **제목(C9)**: `기초자료입력!C22`(D-02 문서 제목) 재사용, 미입력 시 원문 고정 제목으로 대체.
- **가. 대상(C15)**: `기초자료입력!C5`(학년도) 재사용 + 원문 고정 문구("예비 중1(예비 고1) 학생 및 학부모").
- **나. 내용(C16)**: `기초자료입력!C12`(구매명) 재사용 + 원문 고정 문구.
- 학생·학부모 성명·연락처·배정 정보는 원문 자체에 입력 자리가 없어 자동 반영하지 않음(입력·저장·출력
  경로 모두 없음).

## 구현 범위

- `scripts/build_excel_v1_structure.ps1`에 `F-044_사전안내가정통신문안내` 시트를 F-043 뒤에 추가함
  (`$implemented` 해시 등록).
- `scripts/build_excel_v1_vba.ps1`의 `단일출력검증함수명` Select Case에 `F-044` 분기와
  `검증_F044출력가능()`(= `검증_필수값검증()`)를 추가함. 단일 출력(업체별 반복 없음).
- `scripts/verify_excel_v1.ps1`에 필수 시트 목록·개수, 원문 대조(제목·수신·대상 라벨 3건), 공통값 반영
  (학교명·제목·학년도·구매명·붙임 문구), 선택 출력 허용, 개인정보 미출력(본문에 "홍길동"·"010-" 미포함)
  검증을 추가함.

## 오류·수정

- **독립 코드검토에서 발견**: 최초 구현은 `C9`·`C15`·`C16`이 원문 고정 문구만 쓰고 있어 학교명·구매명·
  문서 제목 공통값이 실제 출력에 전혀 반영되지 않는 High 결함이 있었음. 원문 상단 학교명·제목·내용
  수식에 `기초자료입력!C4`/`C22`/`C12` 참조를 추가하고, 골든 테스트 데이터도 실제 동복 구매명과
  일치하도록 수정함.

## 자동 검증

- 수정 후 안전 빌드(구조→VBA→검증) 재실행으로 `artifacts/excel/교복구매_길라잡이_20260919_v12.xlsm`을
  생성했고 `verify_v1_log.txt` 전 항목 PASS(FAIL 0, 기존 F-001~F-025·F-032~F-043·DB 회귀 없음 포함).
- F-044 A4 1쪽 자연 배율 확인: `Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=0`, `VPageBreaks=0`.

## PDF 육안 검토

- 비식별 배포 후보에서 F-044 단일 시트를 PDF로 내보내 160dpi 렌더링으로 직접 확인함. 증빙은
  `artifacts/excel/_diagnostic/F-044_20260919_manual_qa/`(`F044_visual_QA.pdf`,
  `F044_visual_QA-1.png`)에 보관함. A4 1쪽·잘림 없음, 학교명·제목·학년도·구매명 공통값 반영, 수기
  작성 결재란(우편번호·주소·전화·팩스·이메일)은 전부 공란으로 보존됨을 확인함.

## 제한 사항

- F-044 완료로 계획.md 8.1절 4순위(F-044 **완료** → F-045·F-046·F-048 → F-047) 중 첫 항목이 끝남.
  Excel 구현 완료 Form ID는 32종(F-001~F-007·F-014~F-025·F-032~F-044)이며, F-045·F-046·F-047·F-048과
  5~6순위(F-049·F-050, F-008~F-013), 후순위(F-027·F-029·F-030·F-031)가 남아 있음.
- 3순위(F-032~F-040) 작업에서 발견한 범위 밖 결함(`"$변수:"` 콜론 오인식 패턴이 F-007·F-014 등
  기존 "완료" 서식에도 존재)은 이번 F-044 작업 범위가 아니므로 수정하지 않고 그대로 남아 있음.
