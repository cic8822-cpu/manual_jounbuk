# F-004 구매 추진 계획 수립 기안문 구현·검증

- **상태**: `DONE — 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-18
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260918_v8.xlsm`(구조·VBA·검증 스크립트 전체 재실행, `verify_v1_log.txt` 176/176 PASS·FAIL 0)

## 원본 대조

- 원본 HWPX 6쪽 `[2] 교복 학교주관구매 추진 계획 수립(예시)`을 kordoc `parse_pages`로 대조함.
- 구조: F-001과 동일한 기안문 틀(○○학교 머리글, 수신/경유/제목, 결재란)이나 위원 명단 표는 없음.
  본문은 2단락뿐("1. 관련：", "2. 공정하고 투명한 교복 선정 및 합리적인 교복 구매를 위해 …계획을
  붙임과 같이 수립하여 업무 추진에 만전을 기하고자 합니다.") + 붙임 1건(F-005를 가리킴).

## 매핑표 재확인

- `서식_매핑표.md` F-004 행: `C-01~C-06, B-01~B-07, D-01~D-04, R-04~R-06, K-01~K-02` 입력,
  `C-01, C-02, B-01, D-02` 출력, `K-01, K-02` 계산 표시. **원문에는 품목·수량·단가·금액(R-04~R-06,
  K-01~K-02) 필드가 전혀 없음** — F-004는 순수 텍스트 기안문이며 계산 항목이 없음. F-023/F-025에서
  이미 발견된 것과 같은 유형의 매핑표 불일치로 판단함(기록만 하고 매핑표는 수정하지 않음 — P1
  재검토 과제). 실제 출력 열(C-01, C-02, B-01(구매명 대신 구매 계획), D-02)만 원문과 부합하므로
  그 부분만 반영함.

## 구현 범위와 발견한 버그

- `scripts/build_excel_v1_structure.ps1`에 `F-004_구매추진계획수립기안문` 시트를 F-003 뒤에 추가함
  (`$implemented` 해시 등록). F-001/F-007과 동일한 문서번호·시행일자·수신·경유·제목·결재란 구조에
  2단락 본문과 붙임 1건만 반영함.
- **버그 발견·수정**: 1차 PDF 육안 확인에서 "2. 공정하고 투명한…" 문단이 2줄로 줄바꿈되는데 행
  높이가 기본값(1줄분)에 머물러 있어 글자가 위아래로 잘려 보이는 결함을 발견함(F-002의 최초 결함과
  같은 종류 — WrapText만 켜고 행 높이를 명시적으로 늘리지 않음). `$ws.Rows.Item("13:13").AutoFit()`
  + 최소 28pt 보장을 추가해 해결함.
- `scripts/build_excel_v1_vba.ps1`에 `검증_F004출력가능()`(= `검증_필수값검증()`만 요구)과 선택
  분기를 추가함. 단일 출력.
- `scripts/verify_excel_v1.ps1`에 필수 시트 32개, A4 자연 배율 목록, F-004 원문 대조 2건(제목·수신·
  제목란, 본문·붙임 문구 및 기본값 미노출)을 추가함.

## 자동 검증

- 1차 빌드는 전 항목 PASS했으나(176/176) 위 행 높이 결함은 자동 검증(수식/존재 여부만 확인)으로는
  잡히지 않고 PDF 육안 확인에서만 발견됨 — 자동 검증 통과가 조판 완전성을 보장하지 않음을 재확인.
- 수정 후 재빌드로 `artifacts/excel/교복구매_길라잡이_20260918_v8.xlsm`을 생성했고
  `verify_v1_log.txt` 전 항목 PASS(176/176, FAIL 0, 기존 F-001~F-003·F-007·F-014~F-025·DB 회귀
  없음 포함)를 확인함.
- F-004 A4 1쪽 자연 배율 확인: `Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=0`,
  `VPageBreaks=0`.

## PDF 육안 검토

- 비식별 사본(`artifacts/excel/_diagnostic/F-004_20260918_qa/`, 확인 후 삭제)에서 필수값만 입력해
  F-004 단일 PDF를 200dpi로 렌더링·육안 확인함(2회 — 1차 잘림 결함 발견, 2차 수정 확인): 제목
  "교복 학교주관구매 추진 계획 수립(안)", 수신 "내부결재", 제목란 "2026학년도 교복 학교주관구매
  추진 계획(안)", 2단락 본문(학교명·학년도 자동 반영, 2줄 완전 표시), 붙임 "2026학년도 교복
  학교주관구매 추진 계획(안) 1부. 끝.", 결재란까지 A4 1쪽 안에서 잘림 없이 표시됨을 확인함.

## 제한 사항

- F-004 완료는 P3-01 전체 완료를 뜻하지 않음. F-005~F-006과 나머지 Form ID가 남아 있으며 X-06은
  `FAIL`을 유지함.
- `서식_매핑표.md` F-004 행의 R-04~R-06/K-01~K-02(품목·금액) 매핑이 원문과 맞지 않는 불일치를
  기록만 하고 수정하지 않음(P1 재검토 과제).
