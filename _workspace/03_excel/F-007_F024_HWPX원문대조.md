# F-007 · F-024 HWPX 원문 대조 — 읽기 전용 검증

- **상태**: `DONE — 읽기 전용 대조 완료. 수정 없음(수정은 excel-automation-engineer 담당 범위)`
- **작업 범위**: F-007(구매요청기안문), F-024(단가비율표) 이 두 서식만 대조함. P2-01 재개 시도 아님.
- **작업일**: 2026-09-15
- **원본 확인**: `교복4차 (2)/교복 매뉴얼 작성서식0908(안정구 추가).hwpx` (SHA-256: `a5546319f7a6917ca26ba922c9803dfe6b2ec0da6c9ea9d6a7ab96655bf07649`, `.claude/quality-gate.json` 기준값과 일치함)
- **원본 보호**: 원본은 열지 않았으며, `artifacts/hwpx/F007_F024_대조용_사본.hwpx`(원본과 SHA-256 동일한 사본)을 만들어 Kordoc 읽기 전용 파싱(`--format markdown`, `-p` 페이지 범위)만 수행함. 변수 표식 삽입·값 치환 없음.
- **HWPX 파싱 산출물**: `_workspace/02_hwpx/F007_원문_p14.md`(14쪽), `_workspace/02_hwpx/F024_원문_p52-53.md`(52~53쪽, `[9-13]` 참고표 포함)
- **Excel 확인**: `artifacts/excel/교복구매_길라잡이_Excel_v1.xlsm`을 Excel COM `Workbooks.Open(ReadOnly:=true)`로 열어 `F-007_구매요청기안문`, `F-024_단가비율표` 시트의 `UsedRange` 전체 셀을 덤프하고 저장 없이(`Close(SaveChanges:=false)`) 닫음.
- **민감정보 처리**: 두 서식 모두 인명·연락처·계좌 등 개인정보 필드가 없어(F-007/F-024는 서식_인벤토리.md 기준 개인정보 문서가 아님) 별도 마스킹 없이 원문 그대로 기록함.

## 1. F-007 — `[6]` 교복 학교주관구매 구매 요청 (원본 14쪽) vs `F-007_구매요청기안문` 시트

| 항목 | HWPX 원본 | Excel 시트 | 상태 | 비고 |
|---|---|---|---|---|
| 표제 문구 | `[ 6 ] 교복 학교주관구매 구매 요청(예시)` | `교복 학교주관구매 구매 요청(안)` (B3) | 불일치 | 원본은 매뉴얼 예시 표기 `(예시)`, Excel은 `(안)`으로 임의 대체. 핵심 명사구는 유지됨 |
| (경유) 필드 | 존재 (`수신`/`(경유)`/`제목` 3행 표) | 없음 | 누락 | Excel에 `(경유)` 행 자체가 없음 |
| 수신 라벨·값 | `수신` \| `내부결재` | `수  신`(B7) \| `내부결재`(C7) | 일치 | 라벨·값 모두 동일 |
| 제목 라벨 | `제목` | `제  목`(B8) | 일치 | |
| 제목 값 | `○○학년도 교복 학교주관구매 요청` | ` 구매 요청`(C8, 수식 결과) | 부분불일치 | 원본의 `교복 학교주관구매` 문구가 Excel 결과값에서 빠짐(학년도+"구매 요청"만 결합되는 구조로 보임) |
| 관련 문서 | 본문 최상단 `1. 관련：○○학교-○○(20○○.○.○.)` | 결재란 하단 `관련문서:`(B26/C26) | 불일치(위치) | 원본은 본문 첫 항목, Excel은 시트 맨 아래(28행 중 26행)로 이동됨 |
| 서술 문장 | `…교복 구매 계획에 대한 학교운영위원회 심의가 완료되어, 아래와 같이 구매 요청을 하고자 합니다.` | ` 학년도 을(를) 다음과 같이 학교주관구매 방식으로 구매하고자 요청합니다.`(B10) | 불일치 | `학교운영위원회 심의가 완료되어` 근거 문구가 Excel에 없음 |
| 세부 항목 구성 | `가.` 대상 및 수량 / `나.` 예상단가 / `다.` 사양내역 / `라.` 납품기한 (가나다 4항목) | `1.` 구매명 / `2.` 구매 학년 / `3.` 기초금액 / `4.` 계약방식 / `5.` 납품기한 (숫자 5항목, B12~B16) | 불일치 | 항목 개수(4↔5)·라벨·기호체계(가나다↔숫자) 모두 다름. Excel 항목은 `서식_매핑표.md`의 범용 사업 필드(B-01 구매명/B-02 구매 학년/B-03 계약방식/B-04 기초금액/B-06 납품기한)를 그대로 나열한 것으로 보이며, 원본 HWPX 문안(대상·수량, 예상단가, 사양내역)과 직접 대응하지 않음 |
| 붙임 라벨 | `붙임` | `붙임`(B18) | 일치 | |
| 붙임 값 | `교복 사양서 1부. 끝.` (공문서 종결 표기 `끝.` 포함) | `0`(C18, 값 없는 자리표시자) | 불일치 | 구체 문구·공문서 종결 표기 `끝.` 없음(값 미입력 상태이므로 서식 골격만으로는 판단 보류 가능하나, 고정 문구 `교복 사양서 1부`는 원본 고유값이라 확인 필요) |
| 결재란 구성 | `담당자` / `협조자` / `(교장)` 2단 + `시행 ○○○○학교-( )` / `접수` 번호란 + 우편번호(`55065`)/주소/`http://www.jbe.go.kr` + 전화/전송(팩스)/이메일 전체 발신 정보 블록 | `담당`(F28) / `행정실장`(G28) / `교장`(H28) 3칸 라벨만 | 불일치 | 시행·접수 번호, 우편번호, 주소, 전화·팩스, 이메일 블록이 Excel에 전혀 없음. 결재 라인 명칭도 원본의 `협조자`(범용) 대신 `행정실장`(특정 직위)으로 대체됨 |
| 문서번호·시행일자 위치 | 결재란 하단 `시행 ○○○○학교-( )` 한 줄에 결합 표기 | 상단 `문서번호`(B5/C5) / `시행일자`(E5/F5) 별도 필드로 분리, 본문보다 위에 배치 | 불일치 | 공문서 관례상 위치·결합 방식이 원본과 다름 |

**F-007 집계**: 일치 3건 / 부분불일치 1건 / 불일치·누락 8건 (총 12개 비교 항목)

## 2. F-024 — `[9-12]` 품목별 단가 비율표 (원본 52쪽, `[서식 7]`) vs `F-024_단가비율표` 시트

| 항목 | HWPX 원본 | Excel 시트 | 상태 | 비고 |
|---|---|---|---|---|
| 표제 핵심어 | `[ 9-12 ] [서식 7] 교복 품목별 단가 비율표` | `품목별 단가 비율표`(B3) | 부분일치 | 핵심 명사구는 유지되나 서식 번호(`[서식 7]`)·상위 표제(`[9-12]`) 없음 |
| 부제(작성 주체) | `교복 품목별 단가 비율표 (업체 제출양식)` — **업체가 작성해 제출하는 서식**임을 명시 | 없음 | 누락 | Excel은 이 서식을 학교가 입력하는 일반 서식처럼 구성해, 원본이 명시한 "업체 제출양식" 성격이 드러나지 않음 |
| 학교명/학년도/구매명 입력란 | 없음(업체가 채우는 품목·비율 표와 서명란만 존재) | `학교명`(B5)/`학년도`(D5)/`구매명`(F5) 필드 추가 | 불일치(추가) | 원본에 없는 필드를 Excel이 새로 만듦 |
| 표 열 구성 | `품목`(대/소분류 2열) \| `수량` \| `단가비율`(%) \| `비고` | `연번` \| `품목명` \| `수량` \| `단가(원)` \| `금액(원)` \| `비율(%)` | 불일치 | 원본에 없는 `단가(원)`·`금액(원)`·`연번` 열이 추가되고, 원본의 `비고` 열이 사라짐. `기초자료입력` 시트의 범용 품목 반복행(R-04 품목명/R-05 수량/R-06 단가/K-01 금액) 구조를 그대로 가져온 것으로 보이며, 원본이 요구하는 "단가비율(%)" 개념과 다름 |
| 품목 카탈로그(행 구성) | 고정 6개 품목: 동복(`후드 점퍼`/`집업티`/`맨투맨티`/`긴바지(치마)`, rowspan 그룹) · 하복(`반팔티`/`반바지`, rowspan 그룹) | 연번 1~10 빈 범용 행(품목명 미고정, 10행) | 불일치 | 원본은 특정 품목명이 고정된 표(동복/하복 구분 rowspan)이나 Excel은 임의 입력 가능한 범용 10행 표 |
| 비고 열 기준 문구 | `품목별 단가 비율 기준과 ±3% 이상 차이가 나는 품목이 있을 시 기준 미충족` (비고 열에 rowspan 6으로 고지) | 없음(비고 열 자체 없음) | 누락 | 검증 기준 문구가 Excel에 없음 |
| 합계 행 | `합계`(품목 colspan 2) \| 수량 \| 단가비율 \| 비고 (값은 업체가 채우는 빈 칸) | `합계`(C18) \| `F18`=0(금액 합계, 수식) \| `G18`=100(비율 합계 고정값) | 부분일치 | 합계 행 존재 자체는 대응되나 필드 의미(단가비율 vs 금액·비율)가 다름 |
| 서명·날짜·수신 문구 | `20○○년 월 일` / `제안자 성 명 (서명 또는 날인)` / `○○○○학교장 귀하` | 없음 | 누락 | 업체 제출 서식의 서명·제출 대상 문구가 Excel에 전혀 없음 |

**F-024 집계**: 일치 0건 / 부분일치 2건(표제 핵심어, 합계 행 존재) / 불일치·누락 6건 (총 8개 비교 항목)

## 3. 대조 불가능했던 부분

없음. F-007, F-024 모두 원본 페이지(14쪽, 52~53쪽) Kordoc 파싱과 Excel COM 읽기가 정상 완료되어 항목별 대조가 가능했음. `[9-13]` 참고표(단가 비율 기준 예시)는 F-024의 검증 기준 자료로서 함께 확인했으며 Form ID가 부여되지 않은 참조자료이므로 별도 시트 대조 대상은 아님(서식_인벤토리.md 4절).

## 4. 종합

- **일치**: F-007 3건, F-024 0건(부분일치 2건 포함 시 5건)
- **불일치·누락**: F-007 8~9건(부분불일치 포함), F-024 6건
- **근본 원인(관찰)**: 두 시트 모두 원본 HWPX 문안을 직접 대조하지 않고 `서식_매핑표.md`/`입력데이터_사전.md`의 범용 필드 정의(B-01~B-06, R-04~R-06 등)만으로 구조를 구성한 것으로 보임. 특히 F-024는 "단가비율(%)" 개념의 원본 표를 "단가(원)×수량=금액" 계산형 표로 대체해 서식의 목적 자체가 달라짐. F-007은 원본의 4항목(가~라, 대상수량/예상단가/사양내역/납품기한) 대신 5항목(1~5, 구매명/구매학년/기초금액/계약방식/납품기한)을 사용해 항목 구성이 전혀 다름.
- test.md 5.2절의 "구조 초안 — HWPX 원본 문안 대조 검증 필요" 주석은 본 대조로 실제 불일치가 다수 확인되었으므로 유지가 타당함(주석 해제는 excel-automation-engineer의 수정 반영 후 판단).

## 5. 후속 조치(수정은 범위 밖 — excel-automation-engineer 반영용 근거자료)

1. F-007: 세부 항목을 가~라(대상 및 수량/예상단가/사양내역/납품기한)로 재구성하고, 결재란에 시행/접수 번호, 우편번호, 주소, 전화·팩스, 이메일 블록 추가 검토
2. F-024: 열 구성을 `품목(대/소분류)/수량/단가비율(%)/비고`로 재구성하고, 고정 6품목(동복 4종/하복 2종) 및 비고 열의 ±3% 기준 문구, 서명·날짜·수신 문구(제안자 성명, ○○○○학교장 귀하) 반영 검토
3. 두 서식 모두 `(업체 제출양식)` 여부 등 원본이 명시한 작성 주체 표기를 유지할지 여부를 업무 담당자 확인 필요
