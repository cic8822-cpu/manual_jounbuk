# F-046 수요조사 가정통신문 안내 구현·검증

- **상태**: `DONE — 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-19
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260919_v15.xlsm`(구조·VBA·검증 스크립트 전체 재실행, `verify_v1_log.txt` 전 항목 PASS, 271/271)

## 원본 대조

- 원본 HWPX 75쪽 `[18]`을 kordoc으로 이미 추출한 `_workspace/03_excel/F044_048_원문_pages73-77.md`와
  `서식_매핑표.md` F-046 행을 대조함. 단일 물리 페이지.
- 구조: F-044([17])와 완전히 동일한 기안문 구조 — 제목("신입생 예비소집일 교복구매 수요조사 가정통신문
  발송") → 문서번호/시행일자 → 수신(내부결재(사업부서)) → (경유) → 제목(문서 제목 공통값) →
  "1. 관련"(F-044와 동일한 고정 근거 문구) → "2. 발송 사유"(수요조사 목적 서술문) → "가. 대상" →
  "나. 내용" → 붙임 → 결재란(담당/협조자/교장, 시행/접수, 우편번호/주소/전화/팩스/이메일).

## 매핑표 재확인

- `서식_매핑표.md` F-046 행: `C-01~C-06, B-01~B-02, B-07, D-01~D-05` 입력, `C-01, C-02, B-01, D-02` 출력,
  계산 없음, "안내 대상은 비식별 설명만 사용" 비고. F-044 행과 문자 그대로 동일함(둘 다 발송 기안문
  이므로 자연스러운 일치). 원문과 불일치 없음.

## 반영 범위와 재사용 판단

- **학교명(G2)**: `기초자료입력!C4` 재사용, 미입력 시 "○ ○ 학 교".
- **문서번호(C5)·시행일자(F5)**: `기초자료입력!C9`, `C8`(TEXT `yyyy-mm-dd`) 재사용.
- **제목(C9)**: `기초자료입력!C22`(D-02 문서 제목) 재사용, 미입력 시 원문 고정 제목으로 대체. F-046은
  기안문(내부결재용) 서식이라 제목 필드에 절차형 문구가 그대로 쓰여도 자연스러움(F-045와 달리 학부모
  직접 배포용 헤드라인이 아님).
- **가. 대상(C15)**: `기초자료입력!C5`(학년도) 재사용 + 원문 고정 문구("신입생 및 학부모").
- **나. 내용(C16)**: `기초자료입력!C12`(구매명) 재사용 + 원문 고정 문구("교복 학교주관구매 참여 여부
  조사").
- 학생·학부모 성명·연락처·배정 정보는 원문 자체에 입력 자리가 없어 자동 반영하지 않음(입력·저장·출력
  경로 모두 없음).

## 구현 범위

- `scripts/build_excel_v1_structure.ps1`에 `F-046_수요조사가정통신문안내` 시트를 F-045 뒤에 추가함
  (`$implemented` 해시 등록 포함).
- `scripts/build_excel_v1_vba.ps1`의 `단일출력검증함수명` Select Case에 `F-046` 분기와
  `검증_F046출력가능()`(= `검증_필수값검증()`)를 추가함. 단일 출력(업체별 반복 없음).
- `scripts/verify_excel_v1.ps1`에 필수 시트 목록·개수(48→49), A4 1쪽 재확인 목록, 원문 대조(제목·수신·
  대상 라벨 3건), 공통값 반영(학교명·제목·학년도·구매명·붙임 문구), 선택 출력 허용, 개인정보 미출력
  (본문에 "홍길동"·"010-" 미포함) 검증을 F-044 패턴 그대로 추가함.

## 자동 검증

- 안전 빌드(구조→VBA→검증) 재실행으로 `artifacts/excel/교복구매_길라잡이_20260919_v15.xlsm`을 생성했고
  `verify_v1_log.txt` 전 항목 PASS(271/271, FAIL 0, 기존 F-001~F-025·F-032~F-045·DB 회귀 없음 포함).
- F-046 A4 1쪽 자연 배율 확인: `Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`.

## PDF 육안 검토

- 비식별 QA 사본(`artifacts/excel/_diagnostic/F-046_20260919_qa/F046_qa.xlsm`)에서 F-045와 동일한
  골든값을 입력해 F-046 시트만 PDF로 내보내고 160dpi로 렌더링해 직접 확인함. 증빙은
  `artifacts/excel/_diagnostic/F-046_20260919_qa/`(`F046_visual_QA.pdf`, `F046_visual_QA-1.png`)에
  보관함.
- 확인 결과: A4 1쪽·잘림 없음. 제목·수신·관련·대상·내용·붙임이 원문과 일치. 학교명(테스트초등학교)·
  학년도(2026학년도)·구매명(2026학년도 동복 구매)·제목 공통값이 정확히 반영됨. 결재란(우편번호·주소·
  전화·팩스·이메일)은 전부 공란으로 보존됨. 개인정보 형태 문자열 없음.

## 제한 사항

- F-046 완료로 계획.md 8.1절 4순위(F-044·F-045 완료 → **F-046 완료** → F-048 → F-047) 중 세 번째
  항목이 끝남. Excel 구현 완료 Form ID는 34종(F-001~F-007·F-014~F-025·F-032~F-046).
- 다음 대상은 F-048(교복 구매 안내·신청 수량 파악 가정통신문, 신규 구현) → F-047(수요조사 신청 여부
  가정통신문, 개인정보 보안검토 필요).
