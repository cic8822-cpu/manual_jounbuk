# F-045 교복구매 사전 안내 가정통신문 구현·검증

- **상태**: `DONE — 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-19
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260919_v14.xlsm`(구조·VBA·검증 스크립트 전체 재실행, `verify_v1_log.txt` 전 항목 PASS, 266/266)

## 배경

- 2026-09-19 앞선 세션에서 `scripts/build_excel_v1_structure.ps1`(F-045 시트·본문 수식)과
  `scripts/build_excel_v1_vba.ps1`(`검증_F045출력가능()`·선택 출력 분기)이 이미 작성돼 있었으나,
  다음 3가지가 빠져 있어 완료로 볼 수 없었음(`로그.md` "F-045 실제 상태 확인" 항목 참고):
  1. `build_excel_v1_structure.ps1`의 `$implemented` 해시에 `F-045` 미등록 → `서식선택_출력`에서
     선택 불가.
  2. `verify_excel_v1.ps1`에 F-044와 같은 형태의 F-045 전용 자동 검증 없음.
  3. PDF 육안 확인과 이 문서(`F-045_구현검증.md`) 미작성.
- 이번 세션에서 위 3가지 gap을 모두 메움.

## 원본 대조

- 원본 HWPX 74쪽 `[17-1]`을 kordoc으로 이미 추출한 `_workspace/03_excel/F044_048_원문_pages73-77.md`와
  `서식_매핑표.md` F-045 행을 대조함. 단일 물리 페이지, 개인 응답란 없는 순수 안내문.
- 구조: 표제("초 6(예비 중1), 중 3(예비 고1) 교복 구매 사전 안내") → 인사말 → 신입생 배정 전 안내
  → 학교주관구매 제도 근거·상한가 안내 → 착용 여부 등 유의사항 안내 → 감사 인사 → 발행일 → `○○학교장`.

## 매핑표 재확인

- `서식_매핑표.md` F-045 행: `C-01~C-02, B-01~B-02, B-07, D-02, D-05` 입력, `C-01, C-02, B-01, D-02` 출력,
  계산 없음, "개인 응답란 없는 안내문" 비고. 원문과 불일치 없음.
- `_workspace/03_excel/F-045_구현명세.md`에 D-02(문서 제목)를 "표제 또는 보조 표제"로 허용한다고
  사전에 명시돼 있어, 표제 자리에 `기초자료입력!C22`를 그대로 반영하는 기존 수식은 의도된 설계로 확인됨.

## 반영 범위와 재사용 판단

- **표제(B3)**: `기초자료입력!C22`(D-02 문서 제목) 우선 반영, 미입력 시 원문 고정 표제로 대체.
- **본문 도입(B8)**: `기초자료입력!C5`(학년도) + `기초자료입력!C12`(구매명) 재사용 + 원문 고정 문구.
- **학교장(B22)**: `기초자료입력!C4`(학교명) + "장" 재사용, 미입력 시 "○○학교장".
- **발행일(B21)**: `기초자료입력!C8`(발행일) 재사용, 미입력 시 "년  월  일".
- 학생·학부모 성명·연락처·배정 정보는 원문 자체에 입력 자리가 없어 자동 반영하지 않음(입력·저장·출력
  경로 모두 없음).

## 이번 세션에서 메운 gap

1. `scripts/build_excel_v1_structure.ps1`의 `$implemented` 해시에 `"F-045" = "F-045_사전안내가정통신문"`
   항목을 추가함(373행).
2. `scripts/verify_excel_v1.ps1`에 F-045 전용 검증 4건을 추가함: 인사말·맺음말 문구 대조, 학교명·학년도·
   구매명·표제 공통값 반영, `검증_F045출력가능()` 통과, 개인정보("홍길동"·"010-") 미출력.
3. A4 1쪽 자연 배율 재확인 목록(foreach 시트 목록)에 `F-045_사전안내가정통신문`을 추가함.

## 자동 검증

- 안전 빌드(구조→VBA→검증) 재실행으로 `artifacts/excel/교복구매_길라잡이_20260919_v14.xlsm`을 생성했고
  `verify_v1_log.txt` 전 항목 PASS(266/266, FAIL 0, 기존 F-001~F-025·F-032~F-044·DB 회귀 없음 포함).
- F-045 A4 1쪽 자연 배율 확인: `Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`.

## PDF 육안 검토

- 비식별 QA 사본(`artifacts/excel/_diagnostic/F-045_20260919_qa/F045_qa.xlsm`)에서 골든값(학교명=
  테스트초등학교, 학년도=2026, 구매명=2026학년도 동복 구매, 문서 제목=2026학년도 동복 학교주관구매
  요청, 발행일=2026-03-01)을 입력해 F-045 시트만 PDF로 내보내고 160dpi로 렌더링해 직접 확인함. 증빙은
  `artifacts/excel/_diagnostic/F-045_20260919_qa/`(`F045_visual_QA.pdf`, `F045_visual_QA-1.png`)에
  보관함.
- 확인 결과: A4 1쪽·잘림 없음. 본문 인사말·학교주관구매 제도 안내·유의사항·감사 인사 4개 문단이 원문과
  일치. 학년도(2026학년도)·구매명(2026학년도 동복 구매)이 도입 문장에 정확히 반영됨. 발행일(2026. 3.
  1.)과 학교장(테스트초등학교장)이 정확히 반영됨. 개인정보 형태 문자열 없음.
- **표제 반영 방식에 대한 확인(작업 지시에서 별도로 요청한 재확인)**: 골든 데이터의 `기초자료입력!C22`에
  F-007(구매요청기안문) 등에서 쓰는 절차형 제목("2026학년도 동복 학교주관구매 요청")을 넣었더니, F-045
  표제도 그대로 이 문구로 대체되어 렌더링됨(원문 고유 표제 "초 6(예비 중1), 중 3(예비 고1) 교복 구매
  사전 안내"는 C22가 비어 있을 때만 보임). 이 동작 자체는 `F-045_구현명세.md`에 사전 문서화된 의도된
  설계이고 원문 손상이나 개인정보 노출은 아니므로 결함으로 분류하지는 않음. 다만 C22가 여러 Form ID가
  공유하는 단일 입력 필드라서, 담당자가 F-007 계열 문서 작업 중 넣어둔 절차형 제목이 그대로 남아 있으면
  학부모에게 배포되는 이 안내문 표제가 "요청" 어조로 보일 수 있음을 확인함. **권고**: 사용설명서에 "F-045
  출력 전 `기초자료입력!C22`를 이 안내문에 맞는 제목으로 바꾸거나 비워 두면 원문 고유 표제가 자동으로
  쓰인다"는 안내를 추가하는 것을 고려함(이번 세션 범위 밖이라 문구만 여기 기록하고 사용설명서 시트는
  변경하지 않음).

## 제한 사항

- F-045 완료로 계획.md 8.1절 4순위(F-044 완료 → **F-045 완료** → F-046·F-048 → F-047) 중 두 번째
  항목이 끝남. Excel 구현 완료 Form ID는 33종(F-001~F-007·F-014~F-025·F-032~F-045).
- 사용설명서에 표제 필드(C22) 재사용 안내를 추가하는 개선은 이번 세션 범위 밖으로 남겨둠(위 PDF 육안
  검토 항목 참고).
