# F-023 교복 제조 사양서 구현·검증

- **상태**: `DONE — P3-01의 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-18
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260918_v10.xlsm`(v9도 동일 코드로 통과했으나 이후 훅 재검증 실패로 `verify_v1_log.txt`가 삭제되어 재실행함. 코드 변경 없이 재빌드한 것이므로 v9 대상 PDF 육안 확인 결과는 v10에도 그대로 유효함)

## 원본 대조

- 원본 HWPX `[9-11] [서식 6] 납품 교복 제조 사양서`(`artifacts/hwpx/P0-02_원본사본.hwpx`)를 kordoc `parse_pages`(51쪽)와 `extract_tables`로 대조함.
- 구조: 제목 "납품 교복 제조 사양서" → "(제안자: 업체명 )" → 안내문구 "*서식은 입찰 업체 상황에 맞게 변경 사용 가능(사진 등 첨부 가능)" → 소제목 "생활형 교복" → 표(구분·견본 품목·재질·설명 내용 4열, 헤더 1행 + 데이터 6행: 동복 rowspan4(후드 점퍼·집업티·맨투맨티·긴바지) + 하복 rowspan2(반팔티·반바지), 재질·설명 내용 전부 공란) → 각주 "※ 국산섬유원단 사용 여부(한국섬유산업연합회 인증마크 획득) 등 상세히 기재" → 날짜·서명·수신처. 가격·수량 항목은 원문에 없음(단가표가 아님).

## 매핑표 불일치 확인

- `서식_매핑표.md`의 F-023 행(`C-01~C-02, B-01, D-02, R-04~R-06` / 필수 `C-01, C-02, B-01, R-04` / 계산 `K-01`)은 품목명·수량·단가·금액 필드를 가리키나, 원문에는 이런 필드가 전혀 없음. 같은 표의 F-013 행과 문구가 완전히 동일해 F-013에서 복사된 뒤 갱신되지 않은 것으로 판단함.
- 실제 원문과 팀장 지시(F-017~F-022 패턴)에 따라 업체명(R-03, `기초자료입력!B44:B53`)과 학교 공통값(C-01 학교명, C-02 학년도)만 반영하고, 문서 제목은 원문과 동일한 고정 문자열 "납품 교복 제조 사양서"로 반영함.
- `서식_매핑표.md` 자체는 P1 분석 산출물이라 이번 세션에서 직접 수정하지 않았으며, P1 재검토 과제로 남김(우선 팀장·`uniform-workflow-analyst`에게 공유 필요).

## 구현 범위와 개인정보 경계

- `scripts/build_excel_v1_structure.ps1`에 `F-023_교복제조사양서` 시트를 F-022 뒤에 추가함(선택 업체 순번은 `G3`, F-020·F-021과 동일하게 4열 표 우측 여백에 배치). `$implemented` 해시에 `"F-023" = "F-023_교복제조사양서"`를 등록함.
- `scripts/build_excel_v1_vba.ps1`에 F-023 선택출력 분기(업체별 반복), `검증_F023출력가능`/`F023업체행완전한가`/`F023임시시트생성` 함수, 임시 시트 정리 목록(`F023_` 접두어)을 추가함.
- 재질·설명 내용 4개 셀(동복 4행·하복 2행)과 서명란은 F-021·F-022와 동일하게 업체가 직접 작성하는 완전한 공란으로 유지함(공통 DB로 전파하거나 자동 저장하지 않음). 이 서식 자체에는 개인식별정보 필드가 없음(사업자 상세정보 없음).

## 자동 검증

- `scripts/verify_excel_v1.ps1`에 다음을 추가함: 필수 시트 26→27개(F-023 포함), A4 자연 배율 목록에 `F-023_교복제조사양서` 추가, F-023 원문 대조(제목·표 머리글·선택 업체명, 동복 4행/하복 2행 견본 품목, 재질/설명 내용/서명란 공란, 선택 출력 허용) 3건, PDF 선택 목록에 F-023 추가, 업체별 임시 인쇄 시트 정리 검증 1건.
- `run_build_excel_v1_structure_utf8.ps1` 단독 실행(훅과의 동시 실행 없음, 아래 참고)으로 `verify_v1_log.txt` 151개 항목 전부 PASS(FAIL 0건) — F-007·F-014~F-022·F-024·DB 저장/수정/불러오기 회귀 없음 포함.
- F-023 A4 1쪽 자연 배율 확인: `Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=0`, `VPageBreaks=0`.

## 훅 레이스 컨디션 회피

- `.claude/hooks/verify-phase.ps1`이 Write/Edit·Stop 이벤트마다 `verify_excel_v1.ps1`을 백그라운드로 재실행해 안전 빌드와 동시에 도는 경우 검증용 임시 파일 잠금 경합이 재현될 수 있음(F-021·F-022에서 재현된 사전 경고 이슈).
- 회피: 안전 빌드 실행 전 `Get-CimInstance Win32_Process`로 `verify-phase.ps1`/`verify_excel_v1.ps1`/`run_build_excel_v1_structure_utf8.ps1`을 실행 중인 PowerShell 프로세스가 없음을 확인한 뒤 단독 실행함. 클린 로그(151/151 PASS)를 확보함.
- **2차 발생**: PDF 육안 확인 이후 문서 갱신용 Edit들이 계속 훅을 트리거했고, 그중 한 번의 백그라운드 재검증이 실패해 `verify_v1_log.txt`가 삭제된 채 남음(코드는 변경되지 않았고 배포본 v9도 유효했으나 로그 증빙이 없어짐). 좀비 automation-embedding Excel 프로세스 2건을 종료하고 관련 프로세스가 모두 없음을 재확인한 뒤 안전 빌드를 한 번 더 단독 실행해 v10과 클린 로그(151/151 PASS)를 재확보함.

## PDF 육안 검토

- 비식별 사본(`artifacts/excel/_diagnostic/F-023_20260918_qa/`)에서 필수값(기초자료입력!C4=검증초등학교, C5=2026, C12=2026학년도 동복 구매, C22=2026학년도 동복 학교주관구매 요청)과 업체명(B44=검증업체가)만 입력하고 서식선택_출력에서 F-023만 선택해 `검증_선택서식_PDF저장` 매크로로 출력함.
- `output/선택서식_20260918_165516.pdf`(1쪽)에서 제목 "납품 교복 제조 사양서", "(제안자: 검증업체가)", 안내문구, "생활형 교복", 표 머리글(구분·견본 품목·재질·설명 내용), 동복 4행(후드 점퍼·집업티·맨투맨티·긴바지)·하복 2행(반팔티·반바지) 견본 품목이 원문과 일치하고 재질·설명 내용 칸이 전부 공란임을 직접 확인함. 각주 "※ 국산섬유원단...", 날짜 "2026. . .", "제안자 성 명 (서명 또는 날인)"(서명란 공란), "검증초등학교장 귀하"도 정상 반영되고 잘림이 없음을 확인함.
- 진단용 Excel COM 프로세스가 `Quit()` 이후 좀비로 남은 것(사용자 파일과 무관한 automation-embedding 인스턴스)을 확인 후 종료함. 사용자의 기존 무관한 통합문서(이전 세션 기록의 PID 19076)는 이 QA 작업 중 이미 닫혀 있었음(진단 스크립트는 기존 Excel 인스턴스에 연결하지 않고 항상 새 COM 인스턴스를 생성하므로 이번 작업으로 인한 종료는 아님).

## 제한 사항

- F-023 완료는 P3-01 전체 완료를 뜻하지 않음. F-013 및 나머지 40개 Form ID가 남아 있으며 X-06은 `FAIL`을 유지함.
- `서식_매핑표.md`의 F-023 행 불일치(R-04~R-06/K-01)는 이번 세션에서 기록만 하고 수정하지 않음 — P1 분석 담당자의 확인·수정이 필요함.
- 상단 "[검토용] 원본 HWPX [9-11] [서식 6] 대조..." 안내 문구는 F-017~F-022와 동일한 기존 패턴대로 인쇄 영역에 포함되어 PDF에 함께 출력됨(이번 세션에서 새로 도입한 것이 아니며, 별도 변경 요청이 없어 유지함).
