# P2-01 대표 서식 독립 템플릿·고유 토큰 검증

- **상태**: `PARTIAL — 독립 템플릿 3종 생성·구조검증 통과, Kordoc CLI fill 치환 실패로 P2-01은 BLOCKED 유지`
- **작업일**: 2026-09-16
- **원본 보호**: [교복4차 (2)](../../../교복4차%20(2))의 원본 HWPX는 수정하지 않음. 원문 페이지를 작업용 Markdown으로 추출하고, 생성물은 `artifacts/hwpx/P2-01/`에만 저장함.
- **출력물**: [F007_구매요청_템플릿.hwpx](../../../artifacts/hwpx/P2-01/F007_구매요청_템플릿.hwpx), [F024_단가비율표_템플릿.hwpx](../../../artifacts/hwpx/P2-01/F024_단가비율표_템플릿.hwpx), [F013_디자인규격서_템플릿.hwpx](../../../artifacts/hwpx/P2-01/F013_디자인규격서_템플릿.hwpx)

## 1. 수행 결과

| Form ID | 독립 사본 | 토큰 방식 | 구조 검증 | 렌더 | 치환 검증 | 판정 |
|---|---|---|---|---|---|---|
| F-007 | 생성 | `{{F007...}}` | PASS | 1쪽, 표 3개 | CLI `fill -j` 오류 | PARTIAL |
| F-024 | 생성 | `{{F024...}}` | PASS | 1쪽, 표 1개 | 미실행 — F-007 공통 엔진 오류 후 중단 | PARTIAL |
| F-013 | 생성 | `{{F013SCHOOLNAME}}` | PASS | 11쪽, 이미지 55개·표 11개 | 미실행 — F-007 공통 엔진 오류 후 중단 | PARTIAL |

모든 토큰은 밑줄 없이 영문·숫자만 사용함. 현재 Kordoc 생성 결과에서 밑줄은 `\_`로 변환되어 치환 키와 일치하지 않으므로 사용하지 않음.

## 2. 토큰 명세

### F-007 구매 요청

- 학교·수신 정보: `F007SCHOOLNAME`, `F007VIA`, `F007TITLE`, `F007REFERENCEDOCUMENT`, `F007SCHOOLYEAR`
- 구매 내용: `F007TARGETANDQUANTITY`, `F007EXPECTEDUNITPRICE`, `F007SPECIFICATION`, `F007DELIVERYDUEDATE`, `F007ATTACHMENT`
- 결재·발신 정보: `F007DRAFTER`, `F007COOPERATOR`, `F007PRINCIPAL`, `F007DOCUMENTNUMBER`, `F007ISSUEDATE`, `F007ZIPCODE`, `F007ADDRESS`, `F007WEBSITE`, `F007PHONE`, `F007FAX`, `F007EMAIL`

### F-024 단가 비율표

- 행별 수량·비율: `F024QTY01`~`F024QTY06`, `F024RATIO01`~`F024RATIO06`
- 합계·제출 정보: `F024QTYTOTAL`, `F024RATIOTOTAL`, `F024PROPOSALDATE`, `F024PROPOSERNAME`, `F024RECIPIENTSCHOOL`

### F-013 디자인 및 규격서

- 공통 식별값: `F013SCHOOLNAME`
- 이미지·세부 규격·혼용률은 원본 표준 예시를 유지함. 실제 학교별 규격 변경 항목·반복행·이미지 교체는 업무 담당자의 확정과 별도 토큰 설계가 필요함.

## 3. 확인된 제약과 다음 조치

1. `npx -y kordoc@^4 fill <F-007 템플릿> -j <마스킹 JSON>`은 “문서 처리 중 오류”로 종료됨. 두 개의 인식 필드만 담은 최소 JSON에서도 동일하게 실패하여, JSON 크기나 값 범위 문제가 아님을 확인함.
2. 독립 템플릿은 원문 페이지의 내용·표·이미지를 기준으로 새 HWPX로 생성한 작업 사본임. 복합 원본에서 OOXML 구간을 보존해 잘라낸 원본 동일 조판본은 아님.
3. F-013 렌더는 이미지 55개를 인식했으나, 11쪽 복합 표에서 페이지 걸침 경고가 발생함. 한컴 수동 열기와 원본 PDF 쪽별 대조가 필수임.
4. 따라서 P2-01은 완료 처리하지 않음. 다음 우선 조치는 Kordoc `fill` 오류를 재현 가능한 최소 파일로 분리해 원인을 해결하거나, 한컴에서 독립 사본을 승인·저장한 뒤 해당 사본을 대상으로 치환 경로를 다시 검증하는 것임.
